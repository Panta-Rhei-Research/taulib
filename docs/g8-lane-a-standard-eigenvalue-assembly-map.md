# G8 Lane A Standard Eigenvalue Assembly Map

This note records the downstream assembly payoff of the standard lemniscate
eigenvalue source.

## Assembly Chain

If the canonical actual-`xi` scaled value has an exact standard mode index,
then:

```text
canonical scaled value = lemniscate_eigenvalue(mode)
  -> standard spectrum membership
  -> standard spectrum real-valuedness
  -> nonzero-height spectral-parameter reality
  -> paired-eta zero-height closure
  -> actual centered-address balance
  -> actual sigma-fixedness
  -> final live hinge, conditional on accepted tower realization
```

The zero-height branch is already supplied by the paired eta/open-unit closure.
The new standard eigenvalue source closes only the generic real-valuedness of
the standard lemniscate spectrum.

## Remaining Mode-Index Payload

The live theorem is now sharply isolated:

```text
exists/construct modeOf(z) such that
g8ActualXiIotaTauCanonicalScaledOperatorSpectralValue z
  = lemniscate_eigenvalue(modeOf z)
```

This cannot be replaced by finite self-adjoint checks or finite K5 diagnostics.
It is the genuine actual-`xi` to Book III spectrum-membership theorem.

## Boundary

This assembly module may forward to the existing conditional final live hinge,
but it does not use the final hinge, accepted coverage, determinant transfer,
O3, or divisor transfer to prove the mode-index source.
