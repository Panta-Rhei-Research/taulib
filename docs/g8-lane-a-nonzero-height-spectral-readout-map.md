# G8 Lane A Nonzero-Height Spectral Readout Map

## Current theorem-backed state

- The zero-height branch is discharged by the paired eta/open-unit route.
- The nonzero-height branch is isolated as:
  `G8ActualXiNonzeroHeightSpectralParameterReal`.
- The new readout interface packages the exact data needed to supply that
  payload:
  `spectralValue z = orthodoxXiCarrierCenteredQuadratic z.1` and
  `(spectralValue z).im = 0`.

## What this wave closes

`G8ActualXiNonzeroHeightSpectralReadoutContext` proves:

```text
exact readout alignment + real declared spectral value
  -> nonzero-height spectral-parameter reality
  -> height-split Lane A inputs
  -> centered-address B/C balance
  -> actual sigma-fixedness
  -> crossing-mediated readout evidence
```

With a supplied sigma-fixed accepted-tower realization law, the same context
forwards through the existing final live-hinge handoff.

## Remaining payload

The next mathematical target is to instantiate the readout context from
independent Book III operator/spectral machinery:

```text
actual nonzero-height xi carrier
  -> Book III spectral value
  -> exact centered-quadratic alignment
  -> real-valuedness by self-adjoint spectral readout
```

Finite self-adjoint and correspondence checks remain diagnostic unless they
produce the exact real-valued readout theorem above.

## Guardrails

- Do not derive the readout from `RiemannHypothesis`, accepted coverage, the
  final live hinge, O3, full divisor transfer, or completion uniqueness.
- Do not replace exact `BoundaryNF`/centered-quadratic alignment with
  axis-offset equality.
- Do not treat finite-stage diagnostics as global spectral reality.
